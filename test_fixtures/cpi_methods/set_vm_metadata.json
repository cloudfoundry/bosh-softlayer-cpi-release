{
  "method": "set_vm_metadata",
  "arguments": [
    "{{.ID}}",
    {
      "director": "BOSH Director",
      "deployment": "softlayer",
      "tags": "{{.Tags}}"
    }
  ],
  "context": {
    "director_uuid": "{{.DirectorUuid}}"
  }
}
