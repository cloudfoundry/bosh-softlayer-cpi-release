#!/bin/bash

set -e

base=$( cd "$( dirname "$( dirname "$0" )")" && pwd )

echo "DEBUG"

echo "pwd: " $PWD
echo "bin: " $bin
echo "base: " $base

echo "====="

go build -o $base/out/cpi github.com/maximilien/bosh-softlayer-cpi/main
