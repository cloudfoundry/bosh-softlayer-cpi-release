#!/bin/bash

(
	set -e

	base=$( cd "$( dirname "$( dirname "$0" )")" && pwd )
	export GOPATH=$PWD/gopath:$base:$base/Godeps/_workspace:$base_gopath GOBIN=$base_gopath/gobin

	go build -o $base/out/cpi $base/main
)